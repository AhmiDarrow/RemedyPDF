release: 1.4.5 packaging layout, SHA-required updates, hygiene

Fix pip install so the real src package and src.main entry points ship
(instead of flattening core/ui/utils and pointing at the Android main shim).
Default install_update to require a published SHA-256 digest; keep an
explicit opt-out for trusted local/dev channels. Sync version fallbacks,
installer, and buildozer to 1.4.5; drop unused PyPDF2; refresh docs and
tests for the new contracts.

Risk: release channel must publish installer digests (workflow already does).
Smoke: pytest -q; pip install -e . entry point imports src.main.
